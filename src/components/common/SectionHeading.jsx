const SectionHeading = ({ label }) => {
    return (
        <h2 className="text-4xl md:text-5xl font-heading font-semibold uppercase mb-10 text-paper">{label}</h2>
    )
}

export default SectionHeading